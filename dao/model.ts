import {Comment, Consultation, Feedback, LostAndFound, PhoneBook, RepairItem} from "./_init";

const model = {
    PHONE_BOOK: PhoneBook,
    LOST_AND_FOUND: LostAndFound,
    CONSULTATION: Consultation,
    COMMENT: Comment,
    FEEDBACK: Feedback,
    REPAIR_ITEM: RepairItem
}

export default model;