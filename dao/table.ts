import {CanteenEval, DormRepair, LostAndFound, Message, PhoneBook, Reservation, User} from "./_init";

const table = {
    USER: User,
    PHONE_BOOK: PhoneBook,
    LOST_AND_FOUND: LostAndFound,
    MESSAGE: Message,
    // FEEDBACK: Feedback,
    REPAIR: DormRepair,
    RESERVATION: Reservation,
    CANTEEN_EVAL: CanteenEval,
};

export default table;